<table>
	<tr>
		<td><%= gravatar_for @user %></td>
		<td><%= @user.name %> <% if params[:id] == current_user.id.to_s %> <%= link_to "edit your contact details", edit_user_path %><% end %></td>
	</tr>
	<% if params[:id] == current_user.id.to_s %>
	<tr>
		<td></td>
		<td><%= link_to "Add a result", :controller => 'matches', :action => 'new' %></td>			
	</tr>
	<% end %>
	<tr>
		<td>Email</td>
		<td><%= @user.email %></td>
	</tr>
	<tr>
		<td>Mobile</td>
		<td><%= @user.mobile %></td>
	</tr>
	<tr>
		<td>Twitter</td>
		<td><%= @user.twitter %></td>
	</tr>
</table>

<% current_playerdiv = get_playerdiv_by_id(params[:id])
if !current_playerdiv.blank? %>

<strong>Current Opponents:</strong>
<table>
	<tr>
		<th>Name</th>
		<th>Email</th>
		<th>Mobile</th>
	</tr>
			
<% 
players = User.joins(:playerdivs).where(:playerdivs => { :division_id => current_playerdiv.division_id }).where("users.id != ?", params[:id]) 

players.each do |player| %>
	<tr>
		<td><%= link_to player.name, :controller => 'users', :action => 'show', :id => player.id %></td>
		<td><%= mail_to player.email %></td>
		<td><%= player.mobile %></td>
	</tr>
	<% end%>
	</table>
	
<% else %>
<strong>You have no current opponents</strong>
<% end %>
		<p><strong>Recent Results</strong></p>
		<% 
				results = show_users_matches(5)%>
		
		        <table width=100px summary="User results">
			
				
			<% results.each do |r| %>			
				<tr>
					<td><%= User.find(r[0][:user_id]).name %>
					<td><%= r[0][:score]%>
					<td><%= r[1][:score]%>
					<td><%= User.find(r[1][:user_id]).name %>
		        </tr>
				
		<% end %>
		</table>

		      


      </h1>


		<% if ((current_user.admin?) && (params[:id] == current_user.id.to_s)) %>
		<p>
		<h4>Admin Functions</h4>
		<%= link_to "Edit a result", :controller => 'matches', :action => 'index' %><br />
		<%= link_to "Edit a league", :controller => 'playerdivs', :action => 'index' %><br />
		<%= link_to "Edit a player", :controller => 'users', :action => 'index' %><br />
		</p>
		<% end %>
		
	

		
	
<!-- Bust out the opponents -->

<%  #select_tag 'opponent_result',  options_for_select([["1", "1"], ["2", "2"], ["3", "3"]]) %>

<% #end %>

		 

